import 'package:flutter/material.dart';

class AcTempImg extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("temp layout"),
      ),

      // body: Image(image: ),
    );
  }
}
