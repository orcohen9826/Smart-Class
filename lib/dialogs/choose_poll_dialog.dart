import 'package:flutter/material.dart';

class ChoosePoll extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Dialog(
        child: Column(
      children: [ElevatedButton(onPressed: () {}, child: Text("V X pull"))],
    ));
  }
}
